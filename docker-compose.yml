services:
  flights-bff:
    build:
      context: .
      dockerfile: services/flights_bff/Dockerfile
    ports:
      - "${FLIGHTS_BFF_PORT:-8123}:80"
    environment:
      FLIGHTS_BFF_FLIGHTS_BACKEND_URL: "http://flights-backend/api"
      FLIGHTS_BFF_CALLBACK_URL: "http://flights-bff/api"
      FLIGHTS_BFF_AUTH_TOKEN: "${BFF_AUTH_TOKEN:-tok-acme-001}"
    depends_on:
      flights-backend:
        condition: service_healthy

  places-bff:
    build:
      context: .
      dockerfile: services/places_bff/Dockerfile
    ports:
      - "${PLACES_BFF_PORT:-8124}:80"
    environment:
      PLACES_BFF_PLACES_BACKEND_URL: "http://places-backend/api"
      PLACES_BFF_CALLBACK_URL: "http://places-bff/api"
      PLACES_BFF_AUTH_TOKEN: "${BFF_AUTH_TOKEN:-tok-acme-001}"
    depends_on:
      places-backend:
        condition: service_healthy

  places-backend:
    build:
      context: .
      dockerfile: services/places_backend/Dockerfile
    ports:
      - "${PLACES_PORT:-8200}:80"
    environment:
      PLACES_BFF_TOKEN: "${BFF_AUTH_TOKEN:-tok-acme-001}"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost/api/health')"]
      interval: 5s
      timeout: 3s
      retries: 5

  flights-backend:
    build:
      context: .
      dockerfile: services/flights_backend/Dockerfile
    ports:
      - "${FLIGHTS_PORT:-8300}:80"
    environment:
      FLIGHTS_BFF_TOKEN: "${BFF_AUTH_TOKEN:-tok-acme-001}"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost/api/health')"]
      interval: 5s
      timeout: 3s
      retries: 5

  debate-backend:
    build:
      context: .
      dockerfile: services/debate_backend/Dockerfile
    ports:
      - "${BE_PORT:-8000}:80"
    volumes:
      - ${DEBATE_DATA:-./debate-data}:/data
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost/health')"]
      interval: 5s
      timeout: 3s
      retries: 5

  debate-bff:
    build:
      context: .
      dockerfile: services/debate_bff/Dockerfile
    ports:
      - "${FE_PORT:-8001}:80"
    environment:
      DEBATE_BACKEND_URL: "http://debate-backend"
    depends_on:
      debate-backend:
        condition: service_healthy
