[project]
name = "dmap-models"
version = "0.1.0"
description = "Shared wire-protocol models for pyview_map BFF and backends"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dmap_models"]
