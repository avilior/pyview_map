[project]
name = "jrpc_common"
version = "0.1.0"
description = "JSON-RPC 2.0 models and audit tracking"
requires-python = ">=3.14"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["jrpc_common"]
